syntax = "proto3";

message MessageThree {
  message MessageFour {
    
  }

  // 名前空間がmessageの中だけになりその外で同じ名前を使える
  enum EnumTwo {
    ENUM_TWO_UNSPECIFIED = 0;
    //...
  }

  MessageFour four = 1;
  EnumTwo two = 2;
}